import React from 'react';

const Home = () => {
    return (
        <div style={{ marginTop: '50px', marginBottom: '50px' }}>
            <div className="container mb-5 mt-5 d-flex justify-content-center" style={{ height: '300px' }}>
                <span style={{ fontSize: '25px' }}>게시판 (JPA + React)</span>
            </div>
        </div>
    );
};

export default Home;